# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
# dist/ aquí NO son artefactos de build: son las plantillas de integración
# (unit systemd, activación D-Bus). Solo se ignoran los artefactos de empaquetado.
dist/*.whl
dist/*.tar.gz
.venv/
venv/

# Tooling
.ruff_cache/
.pytest_cache/
.mypy_cache/

# Runtime / local
*.log
*.swp

# Material con copyright — NUNCA en el repo público.
# Las hojas de sprites (Pokémon/KH/Fantasy) y sonidos las aporta el usuario
# y viven fuera del repo (p.ej. ~/dotfiles/wpets/sprites, ~/.config/nyx/).
assets/sprites/
assets/sounds/
